問１
ルートブリッジ　switch3
ブリッジプライオリティーの値が一番低いため

問２
switch1 F0/1 指定ポート (switch1 F0/2がルートポートのため)
        F0/2 ルートポート (指定ポートの対向のため)

switch2 F0/1 ルートポート (指定ポートの対向のため)
        F0/2 ブロッキングポート (switch3→switch1→switch2の方がパスコストが低いため)

switch3 E0/1 指定ポート (ルートブリッジのため)
        E0/2 指定ポート (ルートブリッジのため)

switch4 E0/1 指定ポート (switch4 E0/2がルートポートのため)
        E0/2 ルートポート (指定ポートの対向のため)